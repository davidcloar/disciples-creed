---
const navItems = [
	{ href: '/about', label: 'About' },
	{ href: '/creed', label: 'Creed' },
	{ href: '/trainings', label: 'Trainings' },
	{ href: '/resources', label: 'Resources' },
];
---

<header
	class="sticky top-0 z-50 border-b border-gold-deep/20 bg-ink/80 backdrop-blur-md"
	transition:persist
>
	<div class="mx-auto flex max-w-5xl flex-wrap items-center justify-between gap-4 px-6 py-4">
		<a href="/" class="flex items-center gap-3 transition-opacity hover:opacity-80">
			<img src="/images/logo-full.jpg" alt="Disciple's Creed" class="h-10 w-auto rounded" />
			<span class="flex flex-col leading-tight">
				<span class="font-serif text-xl font-medium text-parchment">Disciple's Creed</span>
				<span class="text-xs text-gold/70 tracking-widest uppercase">No Reserve · No Retreat · No Regret</span>
			</span>
		</a>
		<nav aria-label="Primary" class="flex flex-wrap items-center gap-6 text-sm text-parchment">
			{
				navItems.map((item) => (
					<a href={item.href} class="transition-colors hover:text-gold">
						{item.label}
					</a>
				))
			}
		</nav>
		<a
			href="/connect"
			class="rounded-full bg-gold px-5 py-2 text-sm font-medium text-ink transition-all hover:bg-gold-bright hover:shadow-[0_0_16px_rgba(198,161,92,0.35)] active:scale-95"
		>
			Disciple-Making
		</a>
	</div>
</header>
